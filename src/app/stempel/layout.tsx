import type { ReactNode } from "react";

export const metadata = {
  title: "Stempeluhr | Shiftfy",
  description: "Ein- und Ausstempeln",
};

export default function StempelLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
